{
  "name": "simpledugneon",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build": "bun build --compile startUp.ts",
    "test": "bun test",
    "start":"bun run startup.ts"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}
